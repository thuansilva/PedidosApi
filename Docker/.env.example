# ============================================================
#  PedidosApi — Variáveis de ambiente para os composes de ambiente
#  (alpha, beta, rc, stable)
#
#  INSTRUÇÃO:
#    cp Docker/.env.example Docker/.env
#    Edite com seus valores. NUNCA commite o .env gerado.
#
#  Em produção (Azure Web App Container), SQL_PASSWORD é injetado
#  pelo Key Vault ou App Settings — nunca em texto puro.
# ============================================================

SQL_PASSWORD=SuaSenhaAqui
